name: TypeScript server

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: server-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: Server checks
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - run: bun run fmt:check
      - run: bun run check
      - run: bun run test
      - name: Build essential Linux desktop client
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libatspi2.0-dev libjson-glib-dev
          bash scripts/build-linux-client.sh
          ./build/linux-client/sotto --help
      - name: Install native capture dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libpipewire-0.3-dev libusb-1.0-0-dev libsamplerate0-dev pipewire-bin libspa-0.2-modules
      - name: Verify native capture helper and DSP
        run: bash scripts/test-capture.sh core
      # The private synthetic PipeWire daemon occasionally stalls on Ubuntu runners.
      - name: Exercise private PipeWire integration
        continue-on-error: true
        run: bash scripts/test-capture.sh pipewire
      - run: bun run generate:api --check --typescript-only

  swift:
    name: Swift API compatibility
    runs-on: macos-26
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.2
      - name: Verify Swift toolchain
        run: |
          swift --version
          swift -e '
          #if compiler(<6.2)
          #error("Swift 6.2 or newer is required.")
          #endif
          '
      - run: bun install --frozen-lockfile
      - run: bun run generate:api --check
      - name: Test Swift packages without launching the client
        run: swift test

  coordinator:
    name: Compiled server (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            target: bun-darwin-arm64
          - runner: ubuntu-24.04
            target: bun-linux-x64
          - runner: ubuntu-24.04-arm
            target: bun-linux-arm64
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - run: bun run --cwd Server build --target=${{ matrix.target }}
      - name: Smoke compiled executable
        run: ./build/server/sotto-server --help
      - name: Test compiled HTTP server
        run: bun Server/scripts/smoke.ts --executable ./build/server/sotto-server
      - name: Verify native directory lock and compiled FFI
        run: bun test Server/tests/data-lock.test.ts
      - name: Verify embedded correction worker
        run: bun test Server/tests/correction-worker-standalone.test.ts
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sotto-server-coordinator-${{ matrix.target }}
          path: build/server/sotto-server
          if-no-files-found: error

  native:
    name: Native CPU helpers (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            platform: linux-x64
          - runner: ubuntu-24.04-arm
            platform: linux-arm64
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          submodules: recursive
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.2
      - name: Install native build dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential pkg-config libcurl4-openssl-dev
      - name: Build portable CPU package
        env:
          SOTTO_NATIVE: "OFF"
          SOTTO_CUDA: "OFF"
          SOTTO_BUILD_JOBS: "4"
        run: ./scripts/build-server.sh
      - run: ./build/server/sotto-server --help
      - name: Test packaged HTTP server
        run: bun Server/scripts/smoke.ts --executable ./build/server/sotto-server
      - name: Archive complete platform package
        run: tar -czf build/sotto-server-${{ matrix.platform }}-cpu.tar.gz -C build server
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sotto-server-${{ matrix.platform }}-cpu
          path: build/sotto-server-${{ matrix.platform }}-cpu.tar.gz
          if-no-files-found: error
