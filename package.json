{
  "name": "sotto",
  "private": true,
  "packageManager": "bun@1.4.2",
  "workspaces": [
    "Server",
    "LinuxClient"
  ],
  "scripts": {
    "dev:server": "bun run --cwd Server dev",
    "build:server": "bun run --cwd Server build",
    "generate:api": "bun run --cwd Server generate",
    "fmt": "bun run --cwd Server fmt && bun run --cwd LinuxClient fmt",
    "fmt:check": "bun run --cwd Server fmt:check && bun run --cwd LinuxClient fmt:check",
    "check": "bun run --cwd Server typecheck && bun run --cwd LinuxClient typecheck",
    "test": "bun run --cwd Server test && bun run --cwd LinuxClient test"
  }
}
